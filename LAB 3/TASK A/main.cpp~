#include <iostream>

int main()
{
  ifstream fin("Current_Reservoir_levels.tsv");
  if (fin.fail())
  {
    cerr << "File cannot be opened for reading." << std::endl;
    exit(1);
  }
  string junk;
  getline(fin, junk);
  while (fin >> da

  
}
